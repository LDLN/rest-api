# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       Application.ListSyncableObjects

POST	/types/:objType/create					Application.CreateSyncableObject
